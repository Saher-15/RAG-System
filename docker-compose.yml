services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - chroma_data:/app/backend/data
    restart: unless-stopped

volumes:
  chroma_data:
